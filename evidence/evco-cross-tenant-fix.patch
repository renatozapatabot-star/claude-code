diff --git a/src/__tests__/data-integrity-v2.test.ts b/src/__tests__/data-integrity-v2.test.ts
index 7892981..33976c6 100644
--- a/src/__tests__/data-integrity-v2.test.ts
+++ b/src/__tests__/data-integrity-v2.test.ts
@@ -5,6 +5,13 @@
  * and (b) the EVCO tenant filter on every v2-relevant table. Returned
  * rows MUST satisfy both bounds.
  *
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00): resolveTenantColumn/
+ * scopedQuery now REQUIRE a companyId (see src/lib/supabase/scoped-query.ts).
+ * This suite has no portal session to read (it drives the live DB directly,
+ * not through a route) — its own explicit scenario IS the EVCO tenant, so
+ * it passes the literal known slug 'evco' rather than inventing a session
+ * mechanism that doesn't exist for a standalone integration test.
+ *
  * Skipped at run-time when SUPABASE env vars aren't set (CI without
  * service role; local dev without .env.local). Otherwise runs against
  * the live DB.
@@ -23,6 +30,10 @@ import {
 const HAS_ENV = !!process.env.NEXT_PUBLIC_SUPABASE_URL && (!!process.env.SUPABASE_SERVICE_ROLE_KEY || !!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)
 const maybe = HAS_ENV ? describe : describe.skip
 
+// This suite's own scenario IS the EVCO tenant — no session to read here,
+// so the tenant is this literal known slug, not an invented mechanism.
+const EVCO_COMPANY_ID = 'evco'
+
 maybe('v2 scopedQuery — window + tenant integrity', () => {
   let client: ReturnType<typeof getSupabaseServerClient>
   beforeAll(() => { client = getSupabaseServerClient() })
@@ -30,10 +41,10 @@ maybe('v2 scopedQuery — window + tenant integrity', () => {
   for (const table of Object.keys(TABLE_DATE_COLUMN)) {
     const dateCol = TABLE_DATE_COLUMN[table]
     it(`scopedQuery on ${table} returns only rows in [${WINDOW_START}, ${WINDOW_END}) for the resolved tenant`, async () => {
-      const tenant = await resolveTenantColumn(client, table)
+      const tenant = await resolveTenantColumn(client, table, EVCO_COMPANY_ID)
       // It's OK for tenant to be null (table may genuinely not have data for EVCO);
       // the assertion is that scopedQuery returns rows that WOULD pass the window check.
-      const q = await scopedQuery(client, table, dateCol)
+      const q = await scopedQuery(client, table, dateCol, EVCO_COMPANY_ID)
       const { data, error } = await q.builder.limit(5)
       // Allow soft-fail on tables that don't exist yet (newly authored migrations).
       if (error && /relation .* does not exist|undefined column/i.test(error.message)) {
diff --git a/src/app/api/agent/ask/route.ts b/src/app/api/agent/ask/route.ts
index 6f85c60..1c779f0 100644
--- a/src/app/api/agent/ask/route.ts
+++ b/src/app/api/agent/ask/route.ts
@@ -1,12 +1,25 @@
 import { NextRequest, NextResponse } from 'next/server'
-import { getSupabaseServerClient, scopedQuery, scopedQueryNoDate, TABLE_DATE_COLUMN, WINDOW_START, WINDOW_END } from '@/lib/supabase/scoped-query'
+import { cookies } from 'next/headers'
+import { verifySession } from '@/lib/session'
+import { resolveTenantScope } from '@/lib/api/tenant-scope'
+import { getSupabaseServerClient, scopedQuery, scopedQueryNoDate, resolveTenantColumn, TABLE_DATE_COLUMN, WINDOW_START, WINDOW_END } from '@/lib/supabase/scoped-query'
 
 /**
  * EVCO Portal v2 — AI agent endpoint.
  *
  * Wraps Anthropic Claude Sonnet 5 with three tools, all hardcoded to call
- * scopedQuery (window 2024-01-01 → 2027-01-01 + EVCO tenant). The model
- * cannot override either constraint — they're literal in the executor.
+ * scopedQuery (window 2024-01-01 → 2027-01-01 + the CALLING SESSION's own
+ * tenant, per `session.companyId` via `resolveTenantScope`). The model
+ * cannot override any of that — the executor resolves companyId itself,
+ * server-side, before ever building a tool schema or call.
+ *
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00): previously `execTool` called
+ * `scopedQuery`/`scopedQueryNoDate`/`resolveTenantColumn` with no
+ * `companyId` at all, so the (then-hardcoded) EVCO candidate list applied
+ * to every session regardless of tenant. Now the route resolves
+ * `session.companyId` once per request (same `verifySession` +
+ * `resolveTenantScope` pattern used by the other v2 API routes) and
+ * threads it through every query call.
  *
  * System prompt is bilingual; cites table names; refuses to invent numbers.
  *
@@ -77,7 +90,7 @@ interface ToolResult {
   content: string
 }
 
-async function execTool(call: ToolCall): Promise<{ content: string; rows?: number; table?: string }> {
+async function execTool(call: ToolCall, companyId: string): Promise<{ content: string; rows?: number; table?: string }> {
   const sb = getSupabaseServerClient()
   try {
     if (call.name === 'count_rows') {
@@ -86,8 +99,7 @@ async function execTool(call: ToolCall): Promise<{ content: string; rows?: numbe
       // Resolve the tenant filter once; build the count query from scratch
       // (count needs `.select(cols, {count:'exact', head:true})` which can't
       // re-run on a builder where `.select('*')` was already called).
-      const { resolveTenantColumn } = await import('@/lib/supabase/scoped-query')
-      const tenant = await resolveTenantColumn(sb, t)
+      const tenant = await resolveTenantColumn(sb, t, companyId)
       let cq = sb.from(t).select('*', { count: 'exact', head: true })
       if (dateCol) cq = cq.gte(dateCol, WINDOW_START).lt(dateCol, WINDOW_END)
       if (tenant) cq = cq.eq(tenant.column, tenant.value)
@@ -100,7 +112,7 @@ async function execTool(call: ToolCall): Promise<{ content: string; rows?: numbe
       const t: string = call.input.table
       const dateCol: string = call.input.date_column || TABLE_DATE_COLUMN[t]
       const limit = Math.min(50, Number(call.input.limit) || 25)
-      const q = dateCol ? await scopedQuery(sb, t, dateCol) : await scopedQueryNoDate(sb, t)
+      const q = dateCol ? await scopedQuery(sb, t, dateCol, companyId) : await scopedQueryNoDate(sb, t, companyId)
       const { data, error } = await q.builder.limit(limit)
       if (error) return { content: `Error: ${error.message}`, table: t, rows: 0 }
       const rows = (data as any[]) ?? []
@@ -112,7 +124,7 @@ async function execTool(call: ToolCall): Promise<{ content: string; rows?: numbe
     }
     if (call.name === 'get_pedimento_detail') {
       const numero: string = String(call.input.numero || '').trim()
-      const q = await scopedQuery(sb, 'pedimentos', TABLE_DATE_COLUMN.pedimentos)
+      const q = await scopedQuery(sb, 'pedimentos', TABLE_DATE_COLUMN.pedimentos, companyId)
       const { data, error } = await q.builder.eq('numero_pedimento', numero).limit(1)
       if (error) return { content: `Error: ${error.message}`, table: 'pedimentos', rows: 0 }
       if (!data || data.length === 0) return { content: `No se encontró pedimento "${numero}" en la ventana 2024–2026.`, table: 'pedimentos', rows: 0 }
@@ -126,6 +138,16 @@ async function execTool(call: ToolCall): Promise<{ content: string; rows?: numbe
 
 export async function POST(req: NextRequest) {
   try {
+    const token = (await cookies()).get('portal_session')?.value ?? ''
+    const session = await verifySession(token)
+    if (!session) {
+      return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
+    }
+    const companyId = resolveTenantScope(session, req)
+    if (!companyId) {
+      return NextResponse.json({ error: 'No tenant scope' }, { status: 400 })
+    }
+
     const { question, tabContext } = await req.json()
     if (!question || typeof question !== 'string') {
       return NextResponse.json({ error: 'Falta el campo "question".' }, { status: 400 })
@@ -187,7 +209,7 @@ ${Object.entries(TABLE_DATE_COLUMN).map(([t, d]) => `  - ${t} (fecha: ${d})`).jo
       messages = [...messages, { role: 'assistant', content }]
       const toolResults: ToolResult[] = []
       for (const tu of toolUses) {
-        const r = await execTool(tu)
+        const r = await execTool(tu, companyId)
         if (r.table) sourcesAccum.push({ table: r.table, rows: r.rows ?? 0 })
         toolResults.push({ type: 'tool_result', tool_use_id: tu.id, content: r.content })
       }
diff --git a/src/app/v2/anexo24/page.tsx b/src/app/v2/anexo24/page.tsx
index 160d724..42fda14 100644
--- a/src/app/v2/anexo24/page.tsx
+++ b/src/app/v2/anexo24/page.tsx
@@ -1,5 +1,7 @@
+import { cookies } from 'next/headers'
 import { PageWrap } from '@/components/almanac/PageWrap'
 import { AgentDock } from '@/components/almanac/AgentDock'
+import { verifySession } from '@/lib/session'
 import { getSupabaseServerClient, scopedQuery } from '@/lib/supabase/scoped-query'
 import { formatPedimento } from '@/lib/format/pedimento'
 import { formatFraccion } from '@/lib/format/fraccion'
@@ -7,6 +9,18 @@ import { formatMoneyMXN, formatMoneyMXNCompact, formatNumberMX } from '@/lib/for
 
 export const dynamic = 'force-dynamic'
 
+/**
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00) — see src/app/v2/traficos/page.tsx
+ * for the full rationale. This page did not read the portal session before;
+ * it now derives companyId from `portal_session` and refuses to query
+ * without one.
+ */
+async function getCompanyId(): Promise<string> {
+  const token = (await cookies()).get('portal_session')?.value ?? ''
+  const session = await verifySession(token)
+  return session?.companyId ?? ''
+}
+
 interface PartidaRow {
   id?: number | string
   pedimento?: string | null
@@ -41,10 +55,11 @@ interface PedimentoBucket {
   partidas: PartidaRow[]
 }
 
-async function fetchPartidas(): Promise<PartidaRow[]> {
+async function fetchPartidas(companyId: string): Promise<PartidaRow[]> {
+  if (!companyId) return []
   try {
     const sb = getSupabaseServerClient()
-    const q = await scopedQuery(sb, 'anexo24_partidas', 'fecha_pago')
+    const q = await scopedQuery(sb, 'anexo24_partidas', 'fecha_pago', companyId)
     const { data, error } = await q.builder
       .order('fecha_pago', { ascending: false, nullsFirst: false })
       .limit(6000)
@@ -102,7 +117,8 @@ function fmtDateShort(s: string | null | undefined): string {
 }
 
 export default async function Anexo24Page() {
-  const partidas = await fetchPartidas()
+  const companyId = await getCompanyId()
+  const partidas = await fetchPartidas(companyId)
   const buckets = groupByPedimento(partidas)
   const totalValorAduana = buckets.reduce((s, b) => s + b.valor_aduana_mxn, 0)
   const totalValorDolar = buckets.reduce((s, b) => s + b.valor_dolar_total, 0)
diff --git a/src/app/v2/entrada-bodegas/page.tsx b/src/app/v2/entrada-bodegas/page.tsx
index 1ed7f9a..0885353 100644
--- a/src/app/v2/entrada-bodegas/page.tsx
+++ b/src/app/v2/entrada-bodegas/page.tsx
@@ -1,9 +1,23 @@
+import { cookies } from 'next/headers'
 import { PageWrap } from '@/components/almanac/PageWrap'
 import { AgentDock } from '@/components/almanac/AgentDock'
+import { verifySession } from '@/lib/session'
 import { getSupabaseServerClient, scopedQuery, TABLE_DATE_COLUMN } from '@/lib/supabase/scoped-query'
 
 export const dynamic = 'force-dynamic'
 
+/**
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00) — see src/app/v2/traficos/page.tsx
+ * for the full rationale. This page did not read the portal session before;
+ * it now derives companyId from `portal_session` and refuses to query
+ * without one.
+ */
+async function getCompanyId(): Promise<string> {
+  const token = (await cookies()).get('portal_session')?.value ?? ''
+  const session = await verifySession(token)
+  return session?.companyId ?? ''
+}
+
 interface EntradaRow {
   id?: number | string
   cve_entrada?: string | null
@@ -18,10 +32,11 @@ interface EntradaRow {
   recibio_packing_list?: boolean | null
 }
 
-async function fetchEntradas(): Promise<EntradaRow[]> {
+async function fetchEntradas(companyId: string): Promise<EntradaRow[]> {
+  if (!companyId) return []
   try {
     const sb = getSupabaseServerClient()
-    const q = await scopedQuery(sb, 'entradas', TABLE_DATE_COLUMN.entradas)
+    const q = await scopedQuery(sb, 'entradas', TABLE_DATE_COLUMN.entradas, companyId)
     const { data, error } = await q.builder.order(TABLE_DATE_COLUMN.entradas, { ascending: false }).limit(200)
     if (error) {
       console.warn('[v2/entrada-bodegas] supabase error:', error.message)
@@ -42,7 +57,8 @@ function fmtDate(s: string | null | undefined) {
 }
 
 export default async function EntradaBodegasPage() {
-  const rows = await fetchEntradas()
+  const companyId = await getCompanyId()
+  const rows = await fetchEntradas(companyId)
   const today = new Date().toISOString().slice(0, 10)
   const hoy = rows.filter(r => (r.fecha_llegada_mercancia || '').slice(0, 10) === today).length
   const lede = rows.length === 0
diff --git a/src/app/v2/mercancia-bodega/page.tsx b/src/app/v2/mercancia-bodega/page.tsx
index eb5816f..f5cc42e 100644
--- a/src/app/v2/mercancia-bodega/page.tsx
+++ b/src/app/v2/mercancia-bodega/page.tsx
@@ -1,10 +1,24 @@
+import { cookies } from 'next/headers'
 import { PageWrap } from '@/components/almanac/PageWrap'
 import { AgentDock } from '@/components/almanac/AgentDock'
+import { verifySession } from '@/lib/session'
 import { getSupabaseServerClient, scopedQuery, TABLE_DATE_COLUMN } from '@/lib/supabase/scoped-query'
 import { formatNumberMX } from '@/lib/format/money'
 
 export const dynamic = 'force-dynamic'
 
+/**
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00) — see src/app/v2/traficos/page.tsx
+ * for the full rationale. This page did not read the portal session before;
+ * it now derives companyId from `portal_session` and refuses to query
+ * without one.
+ */
+async function getCompanyId(): Promise<string> {
+  const token = (await cookies()).get('portal_session')?.value ?? ''
+  const session = await verifySession(token)
+  return session?.companyId ?? ''
+}
+
 interface MercanciaRow {
   id?: string
   entrada_bodega_id?: string | null
@@ -20,10 +34,11 @@ interface MercanciaRow {
   talon_guia?: string | null
 }
 
-async function fetchMercancia(): Promise<MercanciaRow[]> {
+async function fetchMercancia(companyId: string): Promise<MercanciaRow[]> {
+  if (!companyId) return []
   try {
     const sb = getSupabaseServerClient()
-    const q = await scopedQuery(sb, 'mercancia_bodega', TABLE_DATE_COLUMN.mercancia_bodega)
+    const q = await scopedQuery(sb, 'mercancia_bodega', TABLE_DATE_COLUMN.mercancia_bodega, companyId)
     const { data, error } = await q.builder
       .order('fecha_llegada', { ascending: false, nullsFirst: false })
       .limit(500)
@@ -54,7 +69,8 @@ function daysSince(s: string | null | undefined): number | null {
 }
 
 export default async function MercanciaBodegaPage() {
-  const rows = await fetchMercancia()
+  const companyId = await getCompanyId()
+  const rows = await fetchMercancia(companyId)
   const isEmpty = rows.length === 0
 
   // Hero stats over the loaded inventory
diff --git a/src/app/v2/pedimentos/page.tsx b/src/app/v2/pedimentos/page.tsx
index 346cf96..d1983b1 100644
--- a/src/app/v2/pedimentos/page.tsx
+++ b/src/app/v2/pedimentos/page.tsx
@@ -1,11 +1,25 @@
+import { cookies } from 'next/headers'
 import { PageWrap } from '@/components/almanac/PageWrap'
 import { AgentDock } from '@/components/almanac/AgentDock'
+import { verifySession } from '@/lib/session'
 import { getSupabaseServerClient, scopedQuery, TABLE_DATE_COLUMN } from '@/lib/supabase/scoped-query'
 import { formatPedimento } from '@/lib/format/pedimento'
 import { formatMoneyMXN, formatMoneyMXNCompact, formatNumberMX } from '@/lib/format/money'
 
 export const dynamic = 'force-dynamic'
 
+/**
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00) — see src/app/v2/traficos/page.tsx
+ * for the full rationale. This page did not read the portal session before;
+ * it now derives companyId from `portal_session` and refuses to query
+ * without one.
+ */
+async function getCompanyId(): Promise<string> {
+  const token = (await cookies()).get('portal_session')?.value ?? ''
+  const session = await verifySession(token)
+  return session?.companyId ?? ''
+}
+
 interface PedimentoDetalleRow {
   id?: number
   trafico?: string | null
@@ -30,10 +44,11 @@ interface PedimentoDetalleRow {
   anexo24_pedimento_id?: string | null
 }
 
-async function fetchDetalle(): Promise<PedimentoDetalleRow[]> {
+async function fetchDetalle(companyId: string): Promise<PedimentoDetalleRow[]> {
+  if (!companyId) return []
   try {
     const sb = getSupabaseServerClient()
-    const q = await scopedQuery(sb, 'pedimentos_detalle', TABLE_DATE_COLUMN.pedimentos_detalle)
+    const q = await scopedQuery(sb, 'pedimentos_detalle', TABLE_DATE_COLUMN.pedimentos_detalle, companyId)
     const { data, error } = await q.builder
       .order('fecha_pago', { ascending: false, nullsFirst: false })
       .limit(300)
@@ -62,7 +77,8 @@ function thisMonthCount(rows: PedimentoDetalleRow[]): number {
 }
 
 export default async function PedimentosPage() {
-  const rows = await fetchDetalle()
+  const companyId = await getCompanyId()
+  const rows = await fetchDetalle(companyId)
   const totalValor = rows.reduce((s, r) => s + (r.val_aduana_mn ?? 0), 0)
   const totalDta = rows.reduce((s, r) => s + (r.dta ?? 0), 0)
   const totalIgi = rows.reduce((s, r) => s + (r.igi ?? 0), 0)
diff --git a/src/app/v2/traficos/page.tsx b/src/app/v2/traficos/page.tsx
index e31f0c0..c4d6672 100644
--- a/src/app/v2/traficos/page.tsx
+++ b/src/app/v2/traficos/page.tsx
@@ -1,11 +1,28 @@
+import { cookies } from 'next/headers'
 import { PageWrap } from '@/components/almanac/PageWrap'
 import { AgentDock } from '@/components/almanac/AgentDock'
+import { verifySession } from '@/lib/session'
 import { getSupabaseServerClient, scopedQuery, TABLE_DATE_COLUMN } from '@/lib/supabase/scoped-query'
 import { formatPedimento } from '@/lib/format/pedimento'
 import { formatMoneyMXN } from '@/lib/format/money'
 
 export const dynamic = 'force-dynamic'
 
+/**
+ * SEV-1 fix (2026-07-30 · WO-16 / P4-W1-00): this page previously called
+ * `scopedQuery` with no `companyId` at all — it never read the portal
+ * session. Under the old hardcoded-EVCO_CANDIDATES sniffing that
+ * accidentally worked for EVCO; under any other tenant's session it would
+ * have returned EVCO's rows. Reads `portal_session` the same way the v2
+ * API routes do (`verifySession`, see src/lib/session.ts) and refuses to
+ * query at all when no valid tenant is present.
+ */
+async function getCompanyId(): Promise<string> {
+  const token = (await cookies()).get('portal_session')?.value ?? ''
+  const session = await verifySession(token)
+  return session?.companyId ?? ''
+}
+
 interface TraficoRow {
   id?: number
   trafico?: string
@@ -20,10 +37,11 @@ interface TraficoRow {
   patente?: string | null
 }
 
-async function fetchTraficos(): Promise<TraficoRow[]> {
+async function fetchTraficos(companyId: string): Promise<TraficoRow[]> {
+  if (!companyId) return []
   try {
     const sb = getSupabaseServerClient()
-    const q = await scopedQuery(sb, 'traficos', TABLE_DATE_COLUMN.traficos)
+    const q = await scopedQuery(sb, 'traficos', TABLE_DATE_COLUMN.traficos, companyId)
     const { data, error } = await q.builder.order(TABLE_DATE_COLUMN.traficos, { ascending: false }).limit(200)
     if (error) {
       console.warn('[v2/traficos] supabase error:', error.message)
@@ -49,7 +67,8 @@ function fmtMoney(n: number | null | undefined) {
 }
 
 export default async function TraficosPage() {
-  const rows = await fetchTraficos()
+  const companyId = await getCompanyId()
+  const rows = await fetchTraficos(companyId)
   const today = new Date().toISOString().slice(0, 10)
   const cruzandoHoy = rows.filter(r => (r.fecha_cruce || '').slice(0, 10) === today).length
   const lede = rows.length === 0
diff --git a/src/lib/supabase/scoped-query.ts b/src/lib/supabase/scoped-query.ts
index 990c15d..23ef9f1 100644
--- a/src/lib/supabase/scoped-query.ts
+++ b/src/lib/supabase/scoped-query.ts
@@ -6,12 +6,28 @@ import { createClient, SupabaseClient } from '@supabase/supabase-js'
  * Per the v2 dispatch (2026-05-14): every Supabase query in the portal
  * MUST be scoped to:
  *   1. The 2024–2026 date window (date column varies per table)
- *   2. The EVCO tenant (company_id, sniffed at runtime — see resolveTenantColumn)
+ *   2. The CALLING SESSION's tenant — company_id, derived STRICTLY from
+ *      `session.companyId` (see resolveTenantColumn). No other tenant
+ *      is ever considered, probed, or defaulted to.
  *
- * Tenant filter is sniffed at runtime per Renato IV's adjudication
- * (response: "Both — sniff at runtime"). We probe the table for whichever
- * value matches: company_id='evco' (clean slug per Block EE convention),
- * company_id='9254' (clave-as-slug fallback). Cached per process per table.
+ * SEV-1 FIX (2026-07-30 · WO-16 / P4-W1-00): the previous version probed
+ * a fixed, hardcoded `EVCO_CANDIDATES` list against every table,
+ * regardless of which tenant's session was actually calling. Any signed
+ * -in session — MAFESA included — got the EVCO-shaped filter applied,
+ * because the candidate values were never derived from
+ * `session.companyId` in the first place. That is a real cross-tenant
+ * read: MAFESA (or any other future tenant) could see EVCO's rows, and
+ * vice versa, the moment a non-EVCO session hit a v2 page.
+ *
+ * Fix: `resolveTenantColumn` / `scopedQuery` / `scopedQueryNoDate` now
+ * REQUIRE the caller's `companyId` (sourced from `session.companyId` at
+ * the route/page layer — never inferred, never defaulted). The
+ * candidate {column, value} pairs are no longer a fixed global list —
+ * they are built per-call from THIS tenant's own `companies` row
+ * (`company_id` slug + `clave_cliente` / `globalpc_clave`), reusing the
+ * same slug/clave mapping already codified for writes in
+ * `lib/tenant/resolve-slug.ts`. A session for tenant A can never
+ * resolve, probe for, or match tenant B's identifiers.
  *
  * The AI agent's tool layer wraps this same helper, so the LLM cannot
  * override the window or tenant — the constants below are load-bearing.
@@ -20,53 +36,124 @@ import { createClient, SupabaseClient } from '@supabase/supabase-js'
  *   - vault/00-clawdia/decisions (v2 plan)
  *   - .claude/rules/tenant-isolation.md (HARD invariant — tenant boundary)
  *   - .claude/rules/baseline-2026-04-20.md (I20 — session.companyId is the gate)
+ *   - lib/tenant/resolve-slug.ts (slug/clave mapping this fix reuses)
+ *   - FREIGHT-OS-CANON.md — [WO-16] / [P4-W1-00]
  */
 
 export const WINDOW_START = '2024-01-01'
 export const WINDOW_END = '2027-01-01' // exclusive
 
-// Known EVCO identifiers (sniffed against actual tables).
-const EVCO_CANDIDATES: Array<{ column: string; value: string }> = [
-  { column: 'company_id', value: 'evco' },   // clean slug per Block EE
-  { column: 'company_id', value: '9254' },   // legacy slug-as-clave shape
-  { column: 'cliente_clave', value: '9254' }, // legacy table convention
-  { column: 'clave_cliente', value: '9254' }, // alt legacy spelling
-]
-
-const TENANT_CACHE = new Map<string, { column: string; value: string }>()
+interface TenantCandidate {
+  column: string
+  value: string
+}
 
 export interface TenantResolution {
   column: string
   value: string
 }
 
+// Cache key is `${companyId}::${table}` — NEVER just `table` — so one
+// tenant's resolved {column, value} can never be reused for a different
+// tenant's lookup against the same table. This is part of the SEV-1 fix:
+// the old cache was keyed by table alone, so whichever tenant resolved
+// a table first poisoned the cache for every session after it.
+const TENANT_CACHE = new Map<string, TenantResolution>()
+
+interface CompanyIdentity {
+  company_id: string
+  clave_cliente: string | null
+  globalpc_clave: string | null
+}
+
+/**
+ * Load the calling tenant's OWN identity row from `companies` — the
+ * single source of truth for slug ↔ legacy-clave mapping (the same
+ * table `lib/tenant/resolve-slug.ts` reads for the write path). Scoped
+ * by `.eq('company_id', companyId)` so it is structurally impossible to
+ * fetch a different tenant's identity here.
+ */
+async function loadCompanyIdentity(
+  client: SupabaseClient,
+  companyId: string,
+): Promise<CompanyIdentity | null> {
+  const { data, error } = await client
+    .from('companies')
+    .select('company_id, clave_cliente, globalpc_clave')
+    .eq('company_id', companyId)
+    .maybeSingle<CompanyIdentity>()
+  if (error || !data) return null
+  return data
+}
+
 /**
- * Probe each candidate {column, value} against the given table; cache
- * + return the first that yields > 0 rows. Returns null if no candidate
- * matches (e.g. table has no tenant column at all — like `pedimentos`).
+ * Build the candidate {column, value} pairs for THIS tenant only,
+ * derived from the tenant's own `companies` row — never a hardcoded
+ * global list. A tenant with no `clave_cliente`/`globalpc_clave` (fresh
+ * onboarding, not yet linked to legacy GlobalPC) simply gets fewer
+ * candidates; it never falls back to another tenant's values.
+ */
+function buildTenantCandidates(identity: CompanyIdentity): TenantCandidate[] {
+  const candidates: TenantCandidate[] = [
+    { column: 'company_id', value: identity.company_id },
+  ]
+  const clave = identity.clave_cliente ?? identity.globalpc_clave
+  if (clave) {
+    candidates.push(
+      { column: 'company_id', value: clave },   // legacy slug-as-clave shape
+      { column: 'cliente_clave', value: clave }, // legacy table convention
+      { column: 'clave_cliente', value: clave }, // alt legacy spelling
+    )
+  }
+  return candidates
+}
+
+/**
+ * Probe each candidate {column, value} — derived STRICTLY from
+ * `companyId` (the caller's `session.companyId`, never a hardcoded
+ * candidate list) — against the given table; cache + return the first
+ * that yields > 0 rows. Returns null if no candidate matches (e.g. the
+ * table has no tenant column at all — like `pedimentos`) OR if
+ * `companyId` is missing/empty (no session tenant ⇒ no data, never a
+ * default tenant).
  *
- * SEV-1 history: prior version checked `data !== null` after a
- * `head: true` request. That's always false (head requests don't
- * return rows), so the sniffer always returned null → scopedQuery
- * fell through to NO tenant filter → every v2 page leaked
- * cross-tenant data (Renato IV reported "all clients showing" on
- * 2026-05-15). Now uses `count` from `head: true` and refuses to
- * return data without a tenant binding.
+ * SEV-1 history:
+ *   - v1: checked `data !== null` after a `head: true` request. That's
+ *     always false (head requests don't return rows), so the sniffer
+ *     always returned null → scopedQuery fell through to NO tenant
+ *     filter → every v2 page leaked cross-tenant data (Renato IV
+ *     reported "all clients showing" on 2026-05-15). Patched with a
+ *     `count`-based check.
+ *   - v2 (this file, until 2026-07-30): the count-based check was
+ *     correct, but it probed a fixed, hardcoded `EVCO_CANDIDATES` list
+ *     for EVERY session regardless of caller — so a MAFESA session
+ *     still matched and inherited EVCO's rows. Fixed 2026-07-30
+ *     (WO-16 / P4-W1-00): candidates are now built per-call from the
+ *     session's OWN tenant identity; `companyId` is a required
+ *     parameter, not an ambient assumption.
  */
 export async function resolveTenantColumn(
   client: SupabaseClient,
   table: string,
+  companyId: string,
 ): Promise<TenantResolution | null> {
-  const cached = TENANT_CACHE.get(table)
+  if (!companyId) return null
+
+  const cacheKey = `${companyId}::${table}`
+  const cached = TENANT_CACHE.get(cacheKey)
   if (cached) return cached
-  for (const cand of EVCO_CANDIDATES) {
+
+  const identity = await loadCompanyIdentity(client, companyId)
+  if (!identity) return null
+
+  for (const cand of buildTenantCandidates(identity)) {
     try {
       const { count, error } = await client
         .from(table)
         .select('*', { count: 'exact', head: true })
         .eq(cand.column, cand.value)
       if (!error && typeof count === 'number' && count > 0) {
-        TENANT_CACHE.set(table, cand)
+        TENANT_CACHE.set(cacheKey, cand)
         return cand
       }
     } catch {
@@ -81,37 +168,41 @@ export async function resolveTenantColumn(
  * Date column is REQUIRED — caller picks the right one per-table:
  *   pedimentos.fecha_pago, traficos.fecha_cruce, expediente_documentos.uploaded_at, etc.
  *
+ * `companyId` is REQUIRED and MUST come from `session.companyId` at the
+ * route/page layer — this function does not read the session itself,
+ * it just refuses to guess a tenant when one isn't supplied.
+ *
  * IMPORTANT: PostgrestFilterBuilder is thenable. Returning it directly
  * from an async function would cause `await scopedQuery(...)` to execute
  * the query immediately, defeating the chain. We return a `.builder`
  * accessor on a plain object instead — callers chain off `.builder`.
  *
  * Usage:
- *   const q = await scopedQuery(sb, 'traficos', 'fecha_cruce')
+ *   const q = await scopedQuery(sb, 'traficos', 'fecha_cruce', session.companyId)
  *   const { data, error } = await q.builder.order('fecha_cruce', { ascending: false }).limit(100)
  */
 export async function scopedQuery(
   client: SupabaseClient,
   table: string,
   dateColumn: string,
+  companyId: string,
 ) {
-  const tenant = await resolveTenantColumn(client, table)
+  const tenant = await resolveTenantColumn(client, table, companyId)
   let q = client.from(table).select('*').gte(dateColumn, WINDOW_START).lt(dateColumn, WINDOW_END)
   if (tenant) {
     q = q.eq(tenant.column, tenant.value)
   } else {
     // HARD invariant — no tenant binding = refuse to return any rows.
-    // Better empty-state UX than a SEV-1 cross-tenant leak (the bug we
-    // just fixed). Forces an obviously-impossible filter that returns
-    // zero rows without erroring. The empty result is logged below so
-    // operators see WHICH tables need a tenant column added.
+    // Covers both "no resolvable tenant column" and "no/invalid
+    // companyId" — either way, better an empty-state UX than a
+    // cross-tenant leak (the exact SEV-1 this file fixes).
     q = q.eq(dateColumn, '__no_tenant_column_resolved__')
     if (typeof console !== 'undefined') {
       console.warn(
-        `[scopedQuery] table="${table}" has no resolvable tenant column ` +
-        `(tried: ${EVCO_CANDIDATES.map(c => `${c.column}=${c.value}`).join(', ')}). ` +
-        `Returning empty to prevent cross-tenant leak. Add a tenant column or ` +
-        `add this candidate set to EVCO_CANDIDATES.`,
+        `[scopedQuery] table="${table}" companyId="${companyId || '(missing)'}" has no ` +
+        `resolvable tenant binding. Returning empty to prevent cross-tenant leak. ` +
+        `Add a tenant column, or verify this companyId has a companies row with a ` +
+        `clave_cliente/globalpc_clave mapping.`,
       )
     }
   }
@@ -120,13 +211,15 @@ export async function scopedQuery(
 
 /**
  * Like scopedQuery but for tables WITHOUT a date column (e.g. catalogs).
- * Tenant filter still applied. Same `{builder, tenant}` shape.
+ * Tenant filter still applied and still requires `companyId`. Same
+ * `{builder, tenant}` shape.
  */
 export async function scopedQueryNoDate(
   client: SupabaseClient,
   table: string,
+  companyId: string,
 ) {
-  const tenant = await resolveTenantColumn(client, table)
+  const tenant = await resolveTenantColumn(client, table, companyId)
   let q = client.from(table).select('*')
   if (tenant) {
     q = q.eq(tenant.column, tenant.value)
@@ -134,7 +227,7 @@ export async function scopedQueryNoDate(
     // Same hard contract as scopedQuery — no tenant = empty, never leak.
     q = q.eq('id', '__no_tenant_column_resolved__')
     if (typeof console !== 'undefined') {
-      console.warn(`[scopedQueryNoDate] table="${table}" no tenant column — returning empty`)
+      console.warn(`[scopedQueryNoDate] table="${table}" companyId="${companyId || '(missing)'}" no tenant binding — returning empty`)
     }
   }
   return { builder: q, tenant }
