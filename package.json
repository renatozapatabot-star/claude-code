{
  "name": "cruz",
  "version": "0.1.0",
  "private": true,
  "description": "CRUZ — el sistema operativo del cruce. Cross-border trade OS for EVCO.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "verify": "node scripts/verify.mjs",
    "check": "node --check server.js && node --check data/embarques.js",
    "canon": "node scripts/canon-check.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
